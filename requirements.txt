# Core
mcp>=1.2.0            # MCP server for LM Studio
httpx>=0.27           # async HTTP for fetching and API backends
ddgs>=9.0             # DuckDuckGo search (no API key needed)

# Content extraction
trafilatura>=1.12     # main-body extraction; strips nav/ads/boilerplate
beautifulsoup4>=4.12  # fallback parser, and the analyzer's HTML parser
lxml>=5.0             # parser backend for BeautifulSoup
pypdf>=4.0            # read PDFs in read_url / research

# Site analysis
dnspython>=2.6        # DNS records for /analyze (hosting + mail provider)

# Wikipedia tool uses primp (browser-fingerprint HTTP client) because Wikimedia
# 403s plain httpx. Ships with ddgs, but the wikipedia tool imports it directly.
primp>=1.0

# Optional: free active web-vuln scanner (wapiti_scan). Pure Python, no Java,
# no separate app -- installs where Burp Pro / ZAP can't.
# wapiti3>=3.1

# Optional: JS-rendered pages (read_url fallback). Needs a one-time
# `python -m playwright install chromium` (~300 MB). Only used when
# WEBSEARCH_JS_RENDER=true, so the import is lazy and this stays optional.
playwright>=1.40      # headless-browser render for JS-only sites

# Chat web page (python -m websearch.webchat). These usually arrive with mcp,
# but webchat imports them directly, so pin them.
starlette>=0.40       # ASGI app + routing
uvicorn>=0.30         # ASGI server
sse-starlette>=2.0    # Server-Sent Events streaming to the browser

requests

# APK static triage (skills/apk_analyze.py) — manifest via androguard,
# native .so maps via pyelftools. Optional at import time; skill soft-fails
# the matching section if a package is missing.
androguard>=4.1
pyelftools>=0.31

# APK secret scan (skills/apkleaks.py) — wraps dwisiswant0/apkleaks CLI.
# Needs jadx on PATH (APKLeaks can download it on first run).
apkleaks>=2.6.0